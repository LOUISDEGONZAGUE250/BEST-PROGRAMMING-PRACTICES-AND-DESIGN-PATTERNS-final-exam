# Database configuration
DB_HOST=db
DB_DATABASE=ttbooking
DB_USERNAME=ttuser
DB_PASSWORD=secret

# Optional: override web port
WEB_PORT=8000

# MySQL root password for phpMyAdmin or tooling (do NOT commit real secrets)
MYSQL_ROOT_PASSWORD=rootpassword
